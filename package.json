{
  "name": "badger-badger",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url '/badger-badger/'",
    "format": "semistandard --fix --ignore 'dist/*'",
    "flow": "flow",
    "flow:stop": "flow stop",
    "flow:check": "flow check"  
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.23.9",
    "@babel/plugin-transform-flow-strip-types": "^7.23.3",
    "flow-bin": "^0.228.0",
    "parcel": "^2.11.0",
    "semistandard": "^17.0.0"
  }
}
